import SwiftUI

/// Entry point used by the app when the user is not yet authenticated.
struct OnboardingView: View {
    var body: some View {
        WelcomeView()
    }
}

#Preview {
    OnboardingView()
}
